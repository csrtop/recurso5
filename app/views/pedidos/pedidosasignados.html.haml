%h1 Listado de pedidos Asignados
.page-header
%table.table.table-striped.table-bordered
    %thead
        %tr
            %th Orden
            %th OMS
            %th Socio
            %th Estado del pedido
            %th No Items
            %th Vehiculo
            %th Asignado
            %th Comentario
            %th Tiempo de asignacion
            %th Acciones
            -#%th Hora de asignacion
    %tbody
    - @pedidos.each do |pedido|
        %tr
            %td= pedido.orden
            %td= pedido.OMS
            %td= pedido.socio
            %td= pedido.estado.descripcion
            %td= pedido.no_items
            - pedido.responsables.each do |cm|
                %td= cm.vehiculo
                %td= cm.user.last_name
                %td= cm.comentario
                %td= time_ago_in_words(cm.created_at)
                -#%td= cm.created_at
            -#%td= link_to 'Cambiar Estado / editar', edit_pedido_path(pedido), class: 'btn btn-info'
            %td= link_to 'Ver ubicacion', pedido_posiciones_path(pedido), class: 'btn btn-success'
            -#%td= link_to 'visualizar', pedido_entregas_path(pedido), class: 'btn btn-primary'
            -#%td= link_to 'Asignar Pedido', pedido_responsables_path(pedido), class: 'btn btn-warning'

%br